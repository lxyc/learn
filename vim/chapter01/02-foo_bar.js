var foo = 1
var bar = 'a'
var foobar = foo + bar

-------在行尾添加分号-----

var foo = 1;
var bar = 'a';
var foobar = foo + bar;

组合键位
A == $a 行尾添加
I == ^a 行首插入
o == A 下一行添加
O == ko 上一行添加
C == c$ 从光标到行尾改写
s == cl 改写当前字符
S == ^C 改写整行
